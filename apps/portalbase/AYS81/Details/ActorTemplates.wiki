@usedefaults
h3. Actor Templates

{{datatables_use}}
{{actortemplates ayspath:$$ayspath}}

{% if error is defined %}
    Error : ${error}
{%else%}
    ||Role||Name||Path||Actions||
    {% for template in templates -%}
    | ${template.role} | [${template.name}|ays81/ActorTemplate?aysname=${template.name}&ayspath=${template.aysrepo.path}] | [${template.path} | /ays81/repo?repo=${template.path}] | {{action label:'Delete' deleterow:true id:'action-Deletetemplate' class:'glyphicon glyphicon-trash' data-repository:'${template['repository']}' data-role:'${template['role']}' data-instance:'${template['instance']}'}} |
    {% endfor %}
{%endif%}
