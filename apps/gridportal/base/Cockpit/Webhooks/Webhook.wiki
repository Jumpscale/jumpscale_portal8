@usedefaults

{{timestamp}}

{{webhook key:$$key}}

{% for key, value in payload.items() -%}
    {% if value is mapping %}
        {% for k, v in value.items() -%}
            |*${key}: ${k} *|${v}|
        {% endfor %}
    {% else %}
        |*${key}* | ${value} |
    {% endif %}
{% endfor %}
