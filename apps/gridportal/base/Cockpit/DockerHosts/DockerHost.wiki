@usedefaults

{{dockerhost dockerhost:$$dockerhost ayspath:$$ayspath}}

h3. Docker Host Info:
h5. Name: $$dockerhost

|| Parameter || Value ||
|Instance|${link}|
|State| ${state}|
|Executor| ${executor}|

h5. Children:
|| Type || Name  ||
{% for key, value in children.items() -%}
    |${key}|${value}|
{% endfor %}

h5. Open TCP ports
|| Port Number ||
{% for value in ports -%}
       ${value}
{% else -%}
| |
{% endfor %}

h5. Agent Status
{{agentstats agentid:${agentid}}}