@usedefaults

{{dockerhost dockerhost:$$dockerhost ayspath:$$ayspath}}

h3. Docker Host Info:
h5. Name: $$dockerhost

|| Parameter || Value ||
|Instance|${link}|
|Executor| ${executor}|

h5. State:
${state}

h5. Children:
|| Type || Name  ||
{% for key, value in children.items() -%}
    |${key}|${value}|
{% endfor %}

h5. Open TCP ports
|| Port Number ||
{% for value in ports -%}
       ${value}
{% else -%}
| |
{% endfor %}
