@usedefaults

{{timestamp}}

{{JSAction: runid:$$runid actionkey:$$actionkey}}

h2. Action Details:

|*Name*| ${_name} |
|*RunID*| ${runid} |
|*Key*| ${_key} |
|*Doc*| ${_doc} |
|*Calling Path*| ${calling_path} |
|*State*| ${_state} |
|*State Show*| ${_state_show} |
|*ARGS*| ${_args} |
|*KWARGS*| ${_kwargs} |
|*Die*| ${die} |
|*Path*| ${_path} |
|*Calling Line Number*| ${calling_linenr}|
|*Action Recover*| ${_actionRecover} |
|*Last Code MD5 checksum*| ${_lastCodeMD5} |
|*Last ARGS MD5 checksum*| ${_lastArgsMD5} |

*Parent*
{{html: <a href="/cockpit/jsaction?actionkey=${_parent}&runid=${runid}">${_parentshow}</a>}}


*Dependencies*
{% for dep in _depkeys -%}
    {% for depshow, depunder in dep.items() -%}
        {{html: <a href="/cockpit/jsaction?actionkey=${depunder}&runid=${runid}">${depshow}</a> }}
    {% endfor %}
{% endfor %}

*Source*:
{{code:
    ${_source}
}}


*Cenerator Code*
{{code:
    ${selfGeneratorCode}
}}

*Result*:
{{code:
    ${_result}
}}

*Error*
{{code:
    ${error}
}}

*Traceback*:
{{code:
    ${traceback}
}}

*Stdout Error*:
{{code:
    ${stdouterr}
}}
