@userdefault
{{cockpitinfo}}

h1. Welcom in the Cockpit of ${organization}
h2. SSH access

h3. Over  SSH
Command to connect into the cockpit VM:
{{code:
ssh root@${domain} -p ${ssh_port}
}}

Use the following ssh key to access the cockpit VM:
{{code:
${private_key}
}}

h3. Over http
Address :
[https://${domain}/${shellinbox_url}|https://${domain}/${shellinbox_url}]

h2. REST API
Base URL :
[https://${domain}/api|https://${domain}/api]
Documentation URL :
[https://${domain}/api/apidocs|https://${domain}/api/apidocs/index.html?raml=api.raml]
